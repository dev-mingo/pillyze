import 'package:flutter/material.dart';

class Palette {
  const Palette._();

  static const black = Colors.black;
  static const grey = Colors.grey;
  static const red = Colors.red;
  static const transparent = Colors.transparent;
  static const transparentGrey = Color.fromRGBO(0, 0, 0, 0.75);
  static const white = Colors.white;

  static const splashColor = Color.fromRGBO(0, 0, 0, 0.04);

  static const chartAqua = Color.fromRGBO(102, 221, 220, 1.0);
  static const chartBackgroundRangeAqua = Color.fromRGBO(239, 255, 255, 1.0);
  static const chartGrey = Color.fromRGBO(244, 246, 250, 1.0);
  static const chartRed = Color.fromRGBO(255, 141, 167, 1.0);
  static const chartTextGrey = Color.fromRGBO(196, 200, 206, 1.0);
  static const chartYellow = Color.fromRGBO(255, 205, 0, 1.0);
}
